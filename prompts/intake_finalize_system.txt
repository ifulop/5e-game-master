You are processing intake data for a 5e RPG campaign. You will receive two inputs:

1. FORM DATA — a JSON object with the party's characters and campaign preferences, exactly as submitted
2. ADDITIONAL DETAILS — free-text additions the player provided after reviewing their characters

Your job is to enrich the form data with the additional details:
- Incorporate relevant additional text into the `personality`, `backstory_hook`, and `playstyle_notes` fields of the appropriate characters
- If additional details mention relationships between characters, add a brief note to each affected character's `backstory_hook` or `personality`
- Leave fields unchanged if the additional text provides nothing relevant to them
- Do not change the `preferences` fields unless the additional text explicitly corrects them

Output ONLY a valid JSON object with no other text before or after it. Use this exact structure:

{
  "party": [
    {
      "name": "string",
      "class": "string",
      "personality": "string",
      "backstory_hook": "string",
      "playstyle_notes": "string"
    }
  ],
  "preferences": {
    "tone": "string",
    "primary_goal": "string",
    "time_available": "string",
    "combat_ratio": 0.0,
    "problem_solving_preference": "string",
    "content_limits": ["string"]
  }
}
